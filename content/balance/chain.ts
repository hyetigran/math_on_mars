/** Electric Chain v1: one shared jump budget per volley, no recursive triggers. */
export const CHAIN_BALANCE = {
  range: 120,
  damageFraction: 0.2,
  arcDurationMs: 180,
  arcColor: 0x8cf7ff,
  arcGlowColor: 0x237fbe,
} as const;
